package pages

import (
"goDial/internal/templates/components"
"goDial/internal/templates/layouts"
)

templ Home() {
@layouts.Home("goDial | Home") {
<section class="hero">
    <div class="container">
        <h1>Welcome to goDial</h1>
        <p>Your new favorite application is ready to go!</p>
        @components.Button("Get Started", true, true)
    </div>
</section>

<div class="container">
    <div class="grid">
        @components.Card("Quick Message")
        @components.Card("Send Feedback")
        @components.Card("Contact Us")
    </div>
</div>
}
}
